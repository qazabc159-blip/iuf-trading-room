# Jason PR-H2: 0020 Dedup + UNIQUE Constraint Evidence
**Branch**: `jason/0020-dedup-companies-unique-2026-04-30`
**Date**: 2026-04-30
**Status**: DRAFT — DESTRUCTIVE — requires 楊董 explicit ACK before merge

## WARNING — DESTRUCTIVE MIGRATION

**BLOCKED until 楊董 explicitly ACKs merge AND Railway prod backup is confirmed.**

This migration DELETES duplicate rows from the `companies` table.
Deleted rows cannot be recovered via migration rollback — only from a pg_dump backup.

### Pre-merge Checklist (operator-gated)
- [ ] 楊董 explicit merge ACK in chat
- [ ] `pg_dump $DATABASE_URL -F c -f backup_pre_0020_$(date +%Y%m%d).pgdump` run and confirmed
- [ ] Backup verified: `pg_restore --list backup_pre_0020_*.pgdump | grep companies | wc -l`
- [ ] Run migration on staging first (if staging available)
- [ ] Run 0020 in a transaction (already in BEGIN/COMMIT in migration file)

## Files Changed
- `packages/db/migrations/0020_dedup_companies_unique_ticker.sql` (NEW)
- `packages/db/migrations/0020.down.sql` (NEW)
- `apps/api/src/company-merge.ts` (MODIFIED) — added `upsertCompanyOnConflict()` helper
- `tests/migrations/0020.test.ts` (NEW) — 4 tests (T1-T4)

## Test Count
4 tests in 0020.test.ts (T1-T4) — in-memory logic tests, no live DB required

## Dedup Logic (verified by T1-T2)
1. Partition by `(workspace_id, ticker)`
2. Keep row with most `company_relations` COUNT DESC
3. On tie: keep earliest `created_at`
4. Delete all rn > 1

## Stop-line Check
| Line | Status |
|---|---|
| No secrets in migration | PASS |
| No force-push | PASS |
| No DROP TABLE / TRUNCATE | PASS — DELETE via CTE only |
| Transaction-wrapped | PASS — BEGIN/COMMIT in SQL |
| Down file present | PASS — drops UNIQUE constraint only |
| Backup note in PR | PASS — prominent warning |

## Post-merge: Import Flow
After 0020 is applied, import endpoint can use `upsertCompanyOnConflict()` from
`company-merge.ts` to safely handle re-runs without creating duplicates.
