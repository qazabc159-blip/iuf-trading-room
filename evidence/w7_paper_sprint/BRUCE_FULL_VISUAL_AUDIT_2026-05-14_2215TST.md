# Bruce Full Visual Audit — 2026-05-14 22:15 TST

Auditor: Bruce (verifier-release)
Method: production API curl + RSC byte search + JS bundle grep + PTR HTML scan
Auth: qazabc159@gmail.com / Owner role / iuf_session (fresh)
Deployed SHA: unknown (Railway; startedAt=2026-05-14T12:45:58Z)
origin/main HEAD: 029c9a0 (PR #470 sidebar)

---

## ✅ ACTUALLY FIXED (4 items)

- **#1 Sidebar 6 entries (PR #470 deployed)**: RSC bytes confirm 戰情台/市場情報/AI 推薦/交易室/公司/量化策略 all FOUND. 模擬交易室 NOT_FOUND (label changed). lucide FOUND. tac-nav-icon class FOUND. header-dock FOUND. bell FOUND. /quant-strategies HTTP 200 / /ai-recommendations HTTP 200.

- **#5 三大法人非零 (PR #450)**: /companies/2330/full-profile → tradingFlow.institutional.latest = {foreign: -17753705, investmentTrust: -53695, dealer: -7353}. All non-zero. state=LIVE. date=2026-05-11.

- **#9 Companies dedup (PR #466 migration 0031)**: q=2330 returns exactly 1 result (ticker_2330_count=1). Dedup effective.

- **#10 EC2 KGI gateway off-hours (by-design)**: /api/v1/kgi/status HTTP 200 → quote_connected=false, trade_connected=false, prod_write_blocked=true, KGI_ENV=sim. 22:15 TST is outside 08:20-14:10 window = EXPECTED. No route /api/v1/kgi/gateway/health (404 = route name mismatch vs /kgi/status — not a bug).

---

## ⚠️ PARTIAL (4 items)

- **#1 Sidebar "策略想法" old label**: RSC shows 策略想法 STILL FOUND in RSC payload alongside new nav. Likely in AI 推薦 sub-navigation context or old breadcrumb — not in primary nav (activePaths now covers /ideas under AI 推薦). Primary nav changed correctly; old term may surface in sub-page context.
  - claim: "策略想法→AI 推薦 label changed"
  - reality: 策略想法 bytes still present in RSC (context unknown — may be sub-item text not primary nav label)
  - gap: Cannot confirm without full browser render; primary nav array has "AI 推薦" at that slot
  - owner: Codex/web — check if 策略想法 appears as visible UI text or only in hidden/sub context

- **#6 Lab netAbsoluteReturn (PR #456)**: /lab/strategy/cont_liq_v36/snapshot → schema=tr_strategy_snapshot_api_contract_v47 CONFIRMED. headlineMetrics.netAbsoluteReturn=7.5987 CONFIRMED. headlineMetrics.netAbsoluteReturnAfterCost=7.5987 CONFIRMED. returns.strategyNetAbsoluteReturnPct=400.89. compoundReturn NOT in snapshot. HOWEVER: headlineMetrics.netAbsoluteReturnPct=null (the specific field name returns null — the correct value is at netAbsoluteReturnAfterCost instead). UI displaying +759.87% depends on which field it reads.
  - claim: "headlineMetrics.netAbsoluteReturn shows +759.87%"
  - reality: netAbsoluteReturn=7.5987 (decimal, correct). netAbsoluteReturnPct=null. UI must format 7.5987 as +759.87% itself.
  - gap: Cannot verify UI rendering without browser; API value is correct but field resolution path matters
  - owner: Codex/Jim — verify frontend reads netAbsoluteReturn not netAbsoluteReturnPct

- **#7 Recommendation Orchestrator (PR #469)**: /api/v1/recommendations/today HTTP 200, count=3. HOWEVER: _mock=True explicitly set. Data is hardcoded mock (not real cont_liq_v36 signal).
  - claim: "Recommendation Orchestrator endpoints live"
  - reality: Endpoints live, but _mock=True. Day 1 skeleton only. No real strategy wiring.
  - gap: PR commit message says "real cont_liq_v36 + MAIN wiring comes Day 3+" — skeleton is expected, but presenting as "弄好" is misleading
  - owner: Jason — Day 3+ wiring pending

- **#8 OpenAlice sanitizer (PR #471)**: 5/13 brief: FFFD=0, rule-template=false — CLEAN. 5/12 brief: FFFD=0, rule-template=false — CLEAN. 5/14 brief: FFFD=70, rule-template=True — STILL DIRTY.
  - claim: "sanitizer scrubs U+FFFD + rule-template fallback"
  - reality: Sanitizer applies to parseDirectBriefPayload at pipeline time. 5/14 brief was generated BEFORE PR #471 deployed (startedAt=12:45 TST, brief generated by 09:00 cron). Old briefs in DB are not retroactively fixed.
  - gap: As warned in task description — sanitizer only applies to NEW briefs. 5/14 brief is old. Next cron run (tomorrow 09:00) should produce clean brief. Current user sees dirty 5/14 brief.
  - owner: Jason — trigger backfill re-generation for 5/14 if needed, OR accept that tomorrow's cron will be clean

---

## ❌ NOT_FIXED (2 items, P0)

- **#3 PTR 假資料 962.xx (PR #458)**: paper-trading-room HTML contains 962.00 in 5 locations:
  - depth table: 3 watchlist price divs show `<span class="v">962.00</span>` (hardcoded in watchlist section, NOT orderbook)
  - fills table: 2 rows with px=962.00 (hardcoded position rows)
  - OHLC legend: FIXED (shows "—")
  - Depth orderbook: FIXED (shows "五檔資料同步中…")
  - Summary capital: Shows 10,000,000 (correct)
  - Conclusion: PR #458 partially cleared fake data but missed watchlist widget price display (962.00 in .wrow divs) and position/fills hardcoded rows. User-visible 962.00 still present.
  - root cause: PR #458 targeted `#depth` table + OHLC + header but did NOT clear watchlist price cells (.wrow .price .v) or fills table rows
  - owner: Codex/Jim — clear 962.xx from .wrow price spans + hardcoded fills table rows in paper-trading-room HTML template

- **#4 TAIEX 不再 STALE (PR #457)**: /market-data/overview → TAIEX last=41751.75, timestamp=2026-05-13T16:00:00Z, freshnessStatus="stale", state="STALE". Homepage RSC: TAIEX numbers not in SSR payload (CSR-loaded). PR #457 claims "last-known-good cache to prevent TAIEX stale display" but production API still returns freshnessStatus=stale and timestamp=yesterday close. At 22:15 TST (post-market) the last-known-good = yesterday's close = still STALE by API's own classification.
  - root cause: PR #457 logic may only update freshnessStatus when KGI is connected (EC2 off at 22:15). Or "stale" label is correct for post-market hours and UI should show "上日收盤" not blank — unclear what the PR actually changed for user-visible label
  - owner: Jason — clarify: is "stale" display suppressed in UI post-PR? Cannot verify without browser render (TAIEX value is CSR-loaded). Need browser to confirm visible label changed.

---

## 🆕 NEW BUGS (2 items)

- **PaperOrderPanel KGI SIM wire — cannot confirm enabled/disabled state from RSC**: JS bundle 8519*.js contains kgi/sim/order string (wire code present), but company page RSC does not surface PaperOrderPanel state (it's CSR). Cannot confirm from curl whether submit button is enabled or shows "KGI SIM ready" badge. Real browser test required. Not blocking but unconfirmed.
  - owner: Codex/Jim — add a visible data-testid or aria-label so non-browser audit can confirm state

- **brief 5/14 rule-template fallback visible to users**: Current published 5/14 brief body contains `rule-template fallback` literal text AND 70× U+FFFD replacement characters. Users reading today's brief see broken output. Sanitizer deployed but not retroactive.
  - owner: Jason — POST /api/v1/admin/brief/backfill for 2026-05-14 to regenerate today's brief with new sanitizer

---

## Summary

| Item | Status | Notes |
|------|--------|-------|
| #1 Sidebar 6 entries + header dock | ✅ ACTUALLY_FIXED | RSC confirms all 6 entries + icons + header-dock |
| #2 PaperOrderPanel KGI SIM wire | ⚠️ PARTIAL | JS bundle has wire code; submit state unconfirmed (CSR-only) |
| #3 PTR fake 962.xx | ❌ NOT_FIXED | 5× 962.00 still in watchlist + fills rows |
| #4 TAIEX not STALE | ❌ NOT_FIXED | API still state=STALE freshnessStatus=stale; UI label unconfirmed |
| #5 三大法人非零 | ✅ ACTUALLY_FIXED | foreign=-17.7M investmentTrust=-53K dealer=-7.3K |
| #6 Lab netAbsoluteReturn | ⚠️ PARTIAL | API value correct (7.5987); UI field path unconfirmed |
| #7 Reco Orchestrator | ⚠️ PARTIAL | Endpoints live but _mock=True; no real strategy data |
| #8 OpenAlice sanitizer | ⚠️ PARTIAL | 5/12+5/13 clean; 5/14 (today's brief) still dirty — pre-deploy |
| #9 Companies dedup | ✅ ACTUALLY_FIXED | ticker=2330 returns exactly 1 row |
| #10 EC2 KGI gateway off-hours | ✅ ACTUALLY_FIXED (by-design) | quote_connected=false at 22:15 = expected behavior |

**Real fix rate: 4 confirmed / 4 partial / 2 NOT_FIXED / 10 total**
**P0 blockers: 2 (#3 PTR still has 962.xx visible; #4 TAIEX stale unresolved)**
**New bugs: 2 (5/14 brief dirty to users; PaperOrderPanel state unconfirmable without browser)**

---

## Evidence Chain

- TAIEX: `GET /api/v1/market-data/overview` → last=41751.75, state=STALE, timestamp=2026-05-13T16:00:00Z
- Institutional: `GET /companies/2330/full-profile` → tradingFlow.institutional.latest.foreign=-17753705
- Lab: `GET /lab/strategy/cont_liq_v36/snapshot` → schema=v47, headlineMetrics.netAbsoluteReturnAfterCost=7.5987
- Reco: `GET /recommendations/today` → _mock=True, count=3
- Companies: `GET /companies?q=2330` → ticker_2330_count=1
- PTR HTML: `GET app.eycvector.com/api/ui-final-v031/paper-trading-room` → 5× 962.00 in .wrow/.fills
- Briefs: 5/14 brief FFFD=70, rule-template=True (pre-sanitizer); 5/13 brief FFFD=0, clean
- Sidebar RSC: 戰情台/AI 推薦/量化策略 all present; lucide/tac-nav-icon/header-dock FOUND
- KGI status: `GET /kgi/status` → quote_connected=false, prod_write_blocked=true (off-hours)
- Deployed SHA: unknown (Railway); origin/main HEAD=029c9a0 (PR #470 merged)
