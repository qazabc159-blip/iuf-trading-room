# W6 Day 1 Addendum

**1. Tables**: `paper_orders` / `paper_fills` / `paper_positions`
**2. Purpose**:
- `paper_orders` — order intent + status ledger（每筆下單一列）
- `paper_fills` — fill ledger（一筆 order 可多筆 fill；MVP 先單筆 full fill）
- `paper_positions` — 持倉聚合（每 user×symbol 一列）

**3. Relationships**:
- `paper_fills.order_id` → `paper_orders.id` ON DELETE CASCADE
- `paper_positions` PK = (user_id, symbol)；無 FK（PaperExecutor Day 2 寫入）
- `paper_orders.user_id` / `intent_id` 暫無 FK（Day 2-3 視 schema 演進補）

**4. Migration rollback**: 0015 forward-only，**目前無 .down.sql**（gap，列 D2 review-blocker，非 merge-blocker）。手動回滾：`DROP TABLE paper_positions; DROP TABLE paper_fills; DROP TABLE paper_orders;`

**5. idempotency_key UNIQUE**: `paper_orders.idempotency_key`（migration line 24-25）
**6. ExecutionMode default disabled**: `apps/api/src/domain/trading/execution-mode.ts:19`（env 未設或非 paper/live → disabled）
**7. Kill switch default ON**: `apps/api/src/domain/trading/execution-mode.ts:28-29`（必須明確設 `PAPER_KILL_SWITCH=false` 才解除）

— Elva, 2026-04-29
