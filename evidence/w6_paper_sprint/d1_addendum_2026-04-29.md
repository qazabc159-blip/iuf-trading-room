# W6 Day 1 Addendum

**1. Tables**: `paper_orders`, `paper_fills`, `paper_positions`
**2. Purpose**:
- `paper_orders` — order intent + status ledger（每筆下單一列）
- `paper_fills` — fill ledger（一筆 order 可多筆 fill；MVP 先單筆 full fill）
- `paper_positions` — 持倉聚合（每 user×symbol 一列）

**3. Relationships**:
- `paper_fills.order_id` → `paper_orders.id` ON DELETE CASCADE
- `paper_positions` PRIMARY KEY = (user_id, symbol)，無 FK 到 fills（由 PaperExecutor Day 2 寫入）
- `paper_orders.user_id` / `paper_orders.intent_id` 暫無 FK（Day 2-3 視 schema 演進補）

**4. Migration rollback**: 0015 為 forward-only，**目前無 .down.sql**（gap — 我已記下）。手動回滾 SQL：
```sql
DROP TABLE IF EXISTS paper_positions;
DROP TABLE IF EXISTS paper_fills;
DROP TABLE IF EXISTS paper_orders;
```
建議：Day 2 補 `0015_paper_orders.down.sql`，列入 PR #15 review-blocker（不是 merge-blocker）。

**5. idempotency_key UNIQUE**: `paper_orders.idempotency_key`（migration line 24-25 `paper_orders_idempotency_key_idx`）

**6. ExecutionMode default disabled**: `apps/api/src/domain/trading/execution-mode.ts:19`
```ts
const raw = process.env.EXECUTION_MODE ?? "disabled";
if (raw === "paper" || raw === "live") return raw;
return "disabled";  // 任何其他值（含 unset）→ disabled
```

**7. Kill switch default ON**: `apps/api/src/domain/trading/execution-mode.ts:28-29`
```ts
const raw = process.env.PAPER_KILL_SWITCH ?? "true";
return raw !== "false";  // 必須明確設 "false" 才解除；其他值（含 unset）→ ON
```

— Elva, 2026-04-29
