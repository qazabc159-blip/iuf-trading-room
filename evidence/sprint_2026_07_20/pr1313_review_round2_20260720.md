# PR #1313 Desk Review — ROUND 2 — Pete 2026-07-20

## 0. Context
Round 1 evidence: `evidence/sprint_2026_07_20/pr1313_review.md` (NEEDS_FIX, 2 🔴). This round reviews new commit `a6ee16ff` which claims to fix 🔴#1 (stale fall-through mislabeled "live"). 🔴#2 (track-record API role check) is addressed separately in PR #1314 (reviewed in its own file this round).

## 1. PR Intent (unchanged from round 1)
- `/m` mobile watchlist: honest "closed" fallback via effective-quotes when KGI ticks are empty.
- `/track-record`: owner-only SSR gate (Athena §2).
- New in this round: `deriveEffectiveFallbackCellState()` gets a single source of truth for "live" — only `freshnessStatus === "fresh"` — everything else non-empty degrades to an honest label instead of silently becoming "live".

## 2. Diff Summary (delta since round 1)
- `apps/web/app/m/mobile-quote-effective-fallback.ts`: new `"stale"` variant added to `EffectiveFallbackCellState`; final branch changed from unconditional `return {status:"live",...}` to `if (item.freshnessStatus !== "fresh") return {status:"stale", label: "${sourceLabel}（略舊）", ...}`, with `"live"` now only reachable when `freshnessStatus === "fresh"`. New `sourceLabel()` helper maps `selectedSource` to product-facing Chinese labels (no enum leak).
- `apps/web/app/m/mobile-quote-effective-fallback.test.ts`: +6 tests (stale twse_mis/kgi/manual/tradingview, missing-with-cached-last, existing fresh-is-live case retained).
- `apps/web/app/m/MobileKgiWatchlist.tsx`: `QuoteState` gains `"stale"` variant; `TickerCell` renders it with an amber source label (never green pulse); `fetchEffectiveQuoteFallback()` routes `"stale"` the same as `"closed"` (no longer folded into the else-branch that fabricated `"live"`); `liveCount` recomputed post-fallback-merge so a stale fallback item never inflates the "N/M 活躍" counter.
- LOC delta: +58/-8 roughly (single new commit).
- CI: 5/5 green (`gh pr checks 1313`).

## 3. IUF Blocker Checklist (delta only — see round-1 file for full A–E)

**Priority ordering preserved** — verified `closed_snapshot` and `official_close`+`stale` branches still execute *before* the new generic `freshnessStatus !== "fresh"` branch in `deriveEffectiveFallbackCellState()`. This matters: the desk-approved legitimate states (weekend/holiday closing snapshot, official-close intraday interruption) keep their specific "MM/DD 收盤" / "收盤（即時中斷）" labels and are NOT swallowed by the new generic "來源（略舊）" fallback. PASS — no regression of previously-approved legit states.

**Test coverage genuinely hits the bug path** — read all 6 new test cases: each constructs an `EffectiveMarketQuote` with `freshnessStatus: "stale"` (or `"missing"`) and a non-`official_close` `selectedSource` (`twse_mis`/`kgi`/`manual`/`tradingview`), i.e. exactly the reachable path described in round-1 Blocker #1 (`includeStale:true` → `resolveMarketQuotes()` treats any cached-but-old quote as eligible). Confirmed assertions check `status === "stale"` and the exact label string, not just "not live". PASS — not cosmetic tests.

**UI wiring, not just the pure function** — traced `MobileKgiWatchlist.tsx`: `fetchEffectiveQuoteFallback()` now branches `"closed" || "stale"` into the honest-label path; `liveCount` is computed from `Object.values(next)` *after* the fallback merge (not from the pre-fallback tick results alone), so a stale fallback quote can never inflate "已活躍" count or trigger the green pulsing dot. PASS.

## 4. Findings — Priority Ranked

### 🔴 Blockers
None remaining in this diff. (Round-1 Blocker #2 — track-record API role check — is out of scope for this branch; tracked/resolved in PR #1314, reviewed separately.)

### 🟡 Suggestions

1. **[Copy accuracy, minor]** `MobileKgiWatchlist.tsx`'s header-right summary text still only distinguishes `anyLive` / `anyClosed` / 離線 — a watchlist with only `"stale"` cells (no `"closed"`, no `"live"`) shows the generic "收盤價" label in the header even though the per-cell reason is "來源（略舊）" not an off-hours close. Not a data-honesty violation (each cell's own label is correct), just a slightly imprecise roll-up string. Location: `MobileKgiWatchlist.tsx` `anyClosed` ternary near the `_mob-kgi-right` span. Low priority — the per-cell label is what carries the actual claim.
2. Round-1 🟡#1–4 (SSR fetch timeout / `isOwnerRole` helper reuse / non-owner e2e coverage / fallback-fetch timeout) remain open and are not addressed in this commit — carried forward as non-blocking follow-ups, not re-litigated here.

### 💭 Nits
- The 🔴#1 fix commit's docstring update (explaining exactly what the original bug was and why the new branch exists) is a good append-only pattern — kept the original #1307/#1309/#1310 lineage note intact and added the #1313 postmortem on top rather than rewriting it.

### ✅ Praise
- This is a clean, minimal, single-purpose fix: one new conditional branch, one new UI state, tests added at the exact granularity of the bug (per-source, not just one generic case) — no scope creep into unrelated refactors.
- Author correctly recognized that `closed_snapshot` / `official_close`+`stale` needed to keep priority over the new generic branch rather than just replacing the whole tail with a single `fresh ? live : stale` binary — that binary would have been simpler code but would have demoted the two already-desk-approved states to generic "略舊" labels, losing the more informative "MM/DD 收盤" messaging. Correctly resisted that simplification.
- `sourceLabel()` helper properly product-facing (證交所/凱基/模擬/手動資料/行情) — no raw enum value (`twse_mis`, `kgi`, etc.) ever reaches the UI, consistent with the repo's "禁工程語意" rule.

## 5. Verdict
- [x] APPROVED — no blockers, safe to mark ready

## 6. Suggested Owner for Fixes
- 🟡 #1 (header roll-up copy) → Jim, non-blocking, can bundle into any future `/m` touch-up
- 🟡 round-1 carryover (#1–4) → Jim, backlog, non-blocking

## 7. Re-review Required
NO (this round closes the loop on Blocker #1; Blocker #2 tracked in PR #1314)

## 8. Merge-order note (joint with PR #1314)
See `evidence/sprint_2026_07_20/pr1314_review.md` §6 for the combined recommendation: merge #1313 before #1314. Rationale — #1313's page-level gate returns before any API call is ever made for non-owner sessions, so sequencing #1313 first means non-owner users never see a confusing per-section "登入階段異常" message during the gap between the two merges. Reversing the order (1314 then 1313) is not a security regression (no data leak either way — the pre-existing `/track-record/page.tsx` `AuthIssueNotice` on 401/403 already fails closed), just a transient wording-accuracy gap.

---
Reviewer: Pete
Date: 2026-07-20
Sprint: W6 Day 7 (paper sprint governance fix-chain, round 2)
